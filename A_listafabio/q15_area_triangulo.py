#entrada de dados
base = float(input('Quanto mede a base do triangulo? (em cm) '))
altura = float(input('Quanto mede a altura? (em cm)'))

#processamento
area_triangulo = (base * altura) / 2

#saída de dados
print(f'A área do seu triângulo mede {area_triangulo} centímetros quadrados')